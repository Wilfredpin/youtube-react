import React from 'react';
import ReactDOM from 'react-dom/client'; // Import React and ReactDOM from the 'react' and 'react-dom/client' packages.

const rootEl = document.getElementById('root'); // Get the root element from the HTML document.
const root = ReactDOM.createRoot(rootEl); // Create a root.

root.render(<h1>hello World</h1>); // Render a simple "hello World" message to the root element.


